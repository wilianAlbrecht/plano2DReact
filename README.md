# Plano 2D React

Este projeto é uma aplicação React para manipular peças dentro de um plano 2D usando drag-and-drop, com suporte a múltiplos planos, criação de peças personalizadas, colisão entre elementos e internacionalização (i18n).  
Ele foi desenvolvido para demonstrar domínio de React, TypeScript, gestão de estado, manipulação de eventos e uso da biblioteca `react-dnd`.

---

## 🚀 Tecnologias utilizadas

- **React + TypeScript**
- **Vite** (build rápido e leve)
- **React DnD (react-dnd)** para drag-and-drop
- **i18next** para internacionalização
- **CSS modularizado**
- **uuid** para identificadores únicos

---

## 📦 Instalação

## 📘 Tutorial Completo para Rodar o Projeto com Vite

### 🔧 1. Pré-requisitos

Antes de começar, certifique-se de ter instalado:

 **Node.js 18+**  
  Baixe em: https://nodejs.org/  '''

Para verificar se o Node e o npm estão instalados corretamente:

```
node -v
npm -v
```

📦 2. Clonar o repositório

Abra o terminal e execute:

```
git clone https://github.com/wilianAlbrecht/plano2DReact.git

```

Entre na pasta do projeto:

```
cd plano2DReact

```

📁 3. Instalar as dependências

Este projeto usa npm como gerenciador de pacotes.

Instale as dependências:

```
npm install

```

- React

- TypeScript

- Vite

- React DnD

- i18next

- uuid

- e todas as demais dependências do projeto

⚡ 4. Executar o projeto em modo desenvolvimento

Inicie o servidor local do Vite:

```
npm run dev
```

Você verá algo parecido com:

```
VITE v4.x.x  ready in 300ms

➜  Local:   http://localhost:5173/
➜  Network: http://192.168.x.x:5173/
```

Abra no navegador:

```
http://localhost:5173

```

Se o navegador não abrir automaticamente, basta copiar o link e colar no seu navegador preferido.
